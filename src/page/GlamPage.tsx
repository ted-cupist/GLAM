import DefaultTemplate from "../components/common/DefaultTemplate";
import GlamContainer from "../container/GlamContainer";

const GlamPage = () => {
  return (
    <DefaultTemplate>
      <GlamContainer />
    </DefaultTemplate>
  );
};

export default GlamPage;
