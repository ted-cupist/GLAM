import DefaultTemplate from "../components/common/DefaultTemplate";
import ProfileContainer from "../container/ProfileContainert";

const ProfilePage = () => {
  return (
    <DefaultTemplate>
      <ProfileContainer />
    </DefaultTemplate>
  );
};

export default ProfilePage;
